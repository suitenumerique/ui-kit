import { Canvas, Meta, Story, Source, ArgTypes } from '@storybook/blocks';
import * as Stories from './index.stories';
import { toast } from './DocUtils';
import { ToastProvider } from './ToastProvider';
import { Toast } from './index';

<Meta of={Stories}/>

# Toast

Cunningham provides a powerful Toast component for displaying any kind of information for a defined amount of time.

<Canvas of={Stories.Demo} sourceState="none"/>

## Usage

Toasts are really simple to use, you need to wrap your app inside `CunninghamProvider`.

<Source
  language='tsx'
  dark
  format={false}
  code={`
import { CunninghamProvider } from "@gouvfr-lasuite/ui-components";

const App = () => (
  <CunninghamProvider>
    <InnerApp />
  </CunninghamProvider>
);
  `}
/>

Pass `toastPosition` on `CunninghamProvider` to choose the corner. The toast
slides in from that same side: `bottom-left` (default) arrives from the left,
`bottom-right` sits on the right and arrives from the right.

<Source
  language='tsx'
  dark
  format={false}
  code={`
<CunninghamProvider toastPosition="bottom-right">
  <InnerApp />
</CunninghamProvider>
`}/>

You can also wrap a subtree in `ToastProvider` with `position`.

And then simply use the `toast` function from `useToastProvider` hook.

<Source
  language='tsx'
  dark
  format={false}
  code={`
const InnerApp = () => {
  const { toast } = useToastProvider();

  return (
    <button onClick={() => toast("Hello world!")}>
      Create toast
    </button>
  );
};
`}/>

You can also customize the appearance of the toast with the `toast` function. See the API.

<ArgTypes of={toast} />

## Position

`toastPosition` is one of `top-left`, `top-center`, `top-right`, `bottom-left`
(default), `bottom-center`, `bottom-right`. The slide follows the side: a toast
on the right arrives from the right.

<Canvas of={Stories.PositionBottomRight} />
<Canvas of={Stories.PositionTopRight} />
<Canvas of={Stories.SlideFromRight} />

## Type

Toast can be of different types; each type has a different color. The leading
icon is an arrow by default. Pass `icon` to replace it, or `hideIcon` to hide it.

<Canvas of={Stories.Info} />
<Canvas of={Stories.Success} />
<Canvas of={Stories.Warning} />
<Canvas of={Stories.Error} />
<Canvas of={Stories.Neutral} />

Pass `icon` to replace the default arrow.

<Canvas of={Stories.CustomIcon} />

Pass `hideIcon` to show no leading icon.

<Canvas of={Stories.HiddenIcon} />

## Actions

You can quickly add an action to your toast by passing `primaryLabel` and `primaryOnClick` props to the `toast` function.

<Canvas of={Stories.InfoWithButton} />

## Customize

You can customize the appearance of the toast by passing `actions` when calling `toast` function.

<Canvas of={Stories.InfoCustom} />

`actions` also accepts a list of `{ label, onClick }` descriptors, rendered as borderless
buttons tinted to match the toast type.

<Canvas of={Stories.WithActions} />

## Progress

Pass `progress` to display a completion percentage next to the message.

<Canvas of={Stories.WithProgress} />

## Stacking

Toasts stack: several of them can be on screen at once, the newest at the bottom.

<Canvas of={Stories.Stacked} />

## Updating and dismissing

`toast` returns an id you can use to update or dismiss a toast later. `useToastProvider`
exposes `updateToast` and `dismissToast` for that.

<Source
  language='tsx'
  dark
  format={false}
  code={`
const { toast, dismissToast } = useToastProvider();

const id = toast("File moved", VariantType.SUCCESS, {
  actions: [{ label: "Cancel", onClick: () => {
    undoMove();
    dismissToast(id);
  }}],
});
`}/>

## Extended toast

`toastExtended` displays a collapsible list of items with a progress summary, for file
transfers. It stays on screen until dismissed unless you pass `autoClose`.

<Canvas of={Stories.Extended} />

<Source
  language='tsx'
  dark
  format={false}
  code={`
const { toastExtended, dismissToast } = useToastProvider();

const id = toastExtended({
  summary: "3 documents in transfer",
  progress: 15,
  items: [
    { id: "1", title: "Presentation on Monet", size: "12 GB", mimetype: "application/vnd.ms-powerpoint", status: "completed" },
    { id: "2", title: "Flower wallpaper", mimetype: "image/jpeg", status: "loading" },
  ],
  onClose: () => dismissToast(id),
});
`}/>

## Props

These are the props of `Toast` component which you might not need to create by yourself as the `toast` function does all
the heavy lifting for you.

<ArgTypes of={Toast} />

## Design tokens

Here a the custom design tokens defined by the Toast.

| Token        	| Description                 	|
|---------------	|-----------------------------	|
| icon-size 	| Size of the left icon	|

The toast now takes its colors and typography from the semantic tokens
(`contextuals.background.surface`, `contextuals.content.semantic.*`) like the rest of the
recent components, so `background-color`, `color` and `font-weight` are no longer read.
Slide durations are handled by the transition and no longer read from
`slide-in-duration` / `slide-out-duration`. Those four tokens are still defined, so
overriding them stays valid, but it has no effect.
